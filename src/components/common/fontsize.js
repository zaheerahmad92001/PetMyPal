// import { Platform, Alert, Dimensions } from 'react-native'; // PixelRatio,
// const { height: screenHeight, width: screenWidth } = Dimensions.get('window');
// const scale = Dimensions.get('window').width / 375;

// const getFontSize = (currentFont) => {
	
//       const fontSize = currentFont;
//       const scaleValue = screenWidth / 414;
//     if (screenHeight > 900) {
//       return fontSize * 1.5;
//     }
//     return fontSize * scaleValue;
//   }
  
//   export default getFontSize;
